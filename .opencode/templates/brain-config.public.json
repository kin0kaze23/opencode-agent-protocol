{
  "version": "YOUR_VERSION",
  "name": "YOUR_PROTOCOL_NAME",
  "description": "OpenCode Agent Protocol — public template. Replace all YOUR_* placeholders with your own values.",
  "bootstrap_authority": {
    "bootstrap": "opencode.json loads startup policy surfaces only",
    "runtime_config": "brain-config.json defines defaults, routing, budgets, and enforcement metadata",
    "policy_surface": [
      ".opencode/AGENTS.md",
      ".opencode/rules.md",
      ".opencode/commands/*.md",
      ".opencode/agents/*.md"
    ],
    "conflict_policy": "If runtime config and behavioral policy disagree in a way that could change execution, fail safe, stop the ambiguous action, and surface the mismatch. The behavioral policy surface remains authoritative until patched.",
    "plugins": "augment only; hooks may inject context or notifications but may not redefine authority",
    "conformance": "tests under .opencode/conformance enforce cross-file coherence"
  },
  "prompt_source_of_truth": {
    "orchestrator": ".opencode/global-runtime/prompts/orchestrator.md is intentionally manual-canonical because the Owner prompt is workspace-specific policy, not a reusable helper spec.",
    "helpers": ".opencode/agents/*.md are canonical helper specs; .opencode/global-runtime/prompts/{agent}.md are generated mirrors; ~/.config/opencode/prompts/{agent}.md are installed runtime copies.",
    "sync_script": ".opencode/scripts/sync-opencode-runtime.sh"
  },
  "default_model": "YOUR_PROVIDER/YOUR_MODEL",
  "small_model": "YOUR_PROVIDER/YOUR_SMALL_MODEL",
  "default_model_role": "OWNER ORCHESTRATOR — configure your primary model routing here.",
  "provider_migration": {
    "status": "configure-your-migration-status",
    "secret_source": {
      "provider": "YOUR_SECRET_MANAGER",
      "project": "YOUR_PROJECT",
      "config": "YOUR_CONFIG",
      "required_env": "YOUR_API_KEY_ENV_VAR"
    },
    "active_default_provider": "YOUR_PROVIDER",
    "canonical_registry": ".opencode/model-registry.yaml"
  },
  "skills": {
    "directory": ".opencode/skills",
    "auto_load": {},
    "manual_load_for": {}
  }
}
