# Validation CI — runs on every PR and push to main
# Ensures privacy scan, docs drift, config schema, claims, and protocol conformance pass before merge.
# Validates on both Ubuntu and macOS for multi-environment portability.

name: Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  public-surface-scan:
    name: Privacy Scan (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Run public-surface-scan
        run: bash scripts/public-surface-scan.sh

  docs-drift-validation:
    name: Docs Drift (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Validate docs drift
        run: bash scripts/validate-docs-drift.sh

  config-schema-validation:
    name: Config Schema (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Validate config schema
        run: bash scripts/validate-config-schema.sh

  claims-evidence-validation:
    name: Claims & Evidence (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Validate claims and evidence
        run: bash scripts/validate-claims-evidence.sh

  protocol-validation:
    name: Protocol Conformance (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Protocol Atlas validation
        run: bash .opencode/scripts/validate-protocol-atlas.sh
      - name: Protocol Atlas conformance
        run: bash .opencode/conformance/tests/protocol-atlas.sh
      - name: Production hardening
        run: bash .opencode/conformance/tests/production-hardening.sh
      - name: Loop controller
        run: bash .opencode/conformance/tests/loop-controller.sh
      - name: Model ROI
        run: bash .opencode/conformance/tests/model-roi.sh
